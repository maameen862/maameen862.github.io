# Mohammad Ameenuddin — Data Analyst Portfolio

A Vite + React + TypeScript + Tailwind CSS portfolio website.

## Getting started

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Admin editor

Navigate to `/admin` to edit the portfolio content locally. Changes are saved to the browser's IndexedDB.
